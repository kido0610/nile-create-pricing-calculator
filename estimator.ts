export type EstimateInput = { requirementsDifficulty: "low" | "medium" | "high"; forms: number; reports: number; hasDatabase: boolean; integrations: number; aiFeatures: number; hasOcr: boolean; hasPdf: boolean; hasAdmin: boolean; hasLogin: boolean; hasRoles: boolean; migrationSize: "none" | "small" | "medium" | "large"; conditions: number; exceptionComplexity: "low" | "medium" | "high"; dataReadiness: "good" | "partial" | "poor"; testDifficulty: "low" | "medium" | "high"; trainingHours: number; revisionHours: number; uncertaintyBufferRate: number };
export const estimateSettings = { requirements: { low: 3, medium: 7, high: 12 }, form: 1.5, report: 2.5, database: 8, integration: 6, ai: 5, ocr: 8, pdf: 4, admin: 8, login: 5, roles: 4, migration: { none: 0, small: 4, medium: 10, large: 20 }, condition: .5, exception: { low: 2, medium: 6, high: 12 }, dataReadiness: { good: 0, partial: 4, poor: 8 }, testingRate: { low: .15, medium: .25, high: .4 } } as const;
export const initialEstimateInput: EstimateInput = { requirementsDifficulty: "medium", forms: 1, reports: 1, hasDatabase: false, integrations: 0, aiFeatures: 0, hasOcr: false, hasPdf: false, hasAdmin: false, hasLogin: false, hasRoles: false, migrationSize: "none", conditions: 0, exceptionComplexity: "medium", dataReadiness: "partial", testDifficulty: "medium", trainingHours: 1, revisionHours: 2, uncertaintyBufferRate: 40 };
const n = (v: number) => Math.max(0, Number.isFinite(v) ? v : 0);
export function selectedFeatures(input: EstimateInput) { const items: string[] = []; if (input.forms) items.push("入力フォーム"); if (input.reports) items.push("出力帳票"); if (input.hasDatabase) items.push("データベース"); if (input.integrations) items.push("外部連携"); if (input.aiFeatures) items.push("AI機能"); if (input.hasOcr) items.push("OCR"); if (input.hasPdf) items.push("PDF生成"); if (input.hasAdmin) items.push("管理画面"); if (input.hasLogin) items.push("ログイン"); if (input.hasRoles) items.push("権限管理"); if (input.migrationSize !== "none") items.push("データ移行"); return items; }
export function calculateEstimate(input: EstimateInput) { const s = estimateSettings; const basicDevelopment = n(input.forms) * s.form + n(input.reports) * s.report + (input.hasDatabase ? s.database : 0) + n(input.aiFeatures) * s.ai + (input.hasOcr ? s.ocr : 0) + (input.hasPdf ? s.pdf : 0) + (input.hasAdmin ? s.admin : 0) + (input.hasLogin ? s.login : 0) + (input.hasRoles ? s.roles : 0) + n(input.conditions) * s.condition + s.exception[input.exceptionComplexity] + s.dataReadiness[input.dataReadiness] + s.migration[input.migrationSize]; const requirements = s.requirements[input.requirementsDifficulty]; const externalIntegrations = n(input.integrations) * s.integration; const testing = (basicDevelopment + requirements + externalIntegrations) * s.testingRate[input.testDifficulty]; const standardHours = basicDevelopment + requirements + externalIntegrations + testing + n(input.trainingHours) + n(input.revisionHours); const uncertaintyBuffer = standardHours * Math.min(.5, Math.max(.3, n(input.uncertaintyBufferRate) / 100)); return { basicDevelopment, requirements, externalIntegrations, testing, onboarding: n(input.trainingHours), revisions: n(input.revisionHours), uncertaintyBuffer, minimumHours: standardHours * .8, standardHours, safeHours: standardHours + uncertaintyBuffer, recommendedHours: standardHours + uncertaintyBuffer, features: selectedFeatures(input) }; }
